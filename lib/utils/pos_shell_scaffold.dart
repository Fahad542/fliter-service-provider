import 'package:flutter/material.dart';

/// Key for the Workshop POS shell [Scaffold] so tab bodies (nested scaffolds) can open the drawer.
final GlobalKey<ScaffoldState> kPosShellScaffoldKey = GlobalKey<ScaffoldState>();
